// Built in Objects are prebuilt objects with usable functionalities
let n = new Number(2662); // Number is a prebuilt object

// Some prebuilt objects have static and regular methods, ex: Number has both

console.log(n.toFixed(2)) // Regular method
console.log(Number.isInteger(n)) // Static method